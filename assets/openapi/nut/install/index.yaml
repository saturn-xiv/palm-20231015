post:
  tags:
    - nut
  summary: Initial system.
  requestBody:
    required: true
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: "../users/sign-up/Form.yaml"
  responses:
    "200":
      description: Success.
      content:
        application/json:
          schema:
