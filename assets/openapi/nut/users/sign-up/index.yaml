post:
  tags:
    - nut
  summary: Sign up by email.
  requestBody:
    required: true
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: "./Form.yaml"
  responses:
    "200":
      description: Success.
      content:
        application/json:
          schema:
            $ref: "../../Ok.yaml"
