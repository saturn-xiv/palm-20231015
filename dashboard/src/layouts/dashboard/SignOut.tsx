import { Avatar } from 'antd';
import { LogoutOutlined } from '@ant-design/icons';

const Widget = () => {
  return <Avatar shape="square" size="small" icon={<LogoutOutlined />} />;
};

export default Widget;
