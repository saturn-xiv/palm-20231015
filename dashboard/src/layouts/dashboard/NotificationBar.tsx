import { Avatar } from 'antd';
import { NotificationOutlined } from '@ant-design/icons';

const Widget = () => {
  return <Avatar shape="square" size="small" icon={<NotificationOutlined />} />;
};

export default Widget;
