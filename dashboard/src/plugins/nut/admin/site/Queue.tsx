import { Card } from "antd";
import { FormattedMessage } from "react-intl";

const Widget = () => {
  // TODO
  return (
    <Card title={<FormattedMessage id="nut.admin.site.queue.title" />}></Card>
  );
};

export default Widget;
